exclude_paths:
  - docs
  - molecule
skip_list:
  - no-handler
  - yaml[line-length]
  - yaml[empty-lines]
  - jinja[spacing]
  - command-instead-of-shell
  - template-instead-of-copy # copy with 'content' that contains variables
  - name[template] # variables in task names that are not at the end

