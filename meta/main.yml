galaxy_info:
  author: DrPsychick
  role_name: ansible_testing
  namespace: drpsychick
  description: role for molecule to spin up systemd docker containers or libvirt VMs to test your ansible roles with
  license: MIT
  min_ansible_version: "2.10"

  platforms:
    - name: Ubuntu
    - name: Fedora

  galaxy_tags:
    - ansible
    - docker
    - testing
    - libvirt
    - molecule

  # dependencies:
  #   "community.docker": ">=3.10.0"
  #   "community.general": ">=9.0.0"
  #   "community.libvirt": ">=1.3.0"
